/* const router = require("mongoose").Router();
const journeyController = require("../controller/journey.controller");

router.get("/list", journeyController.getJourneyList);

router.get("/details/:id", journeyController.getJourneyById);

router.post("/create", journeyController.createJourney);

module.exports = router;
 */
